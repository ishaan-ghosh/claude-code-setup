{
  "name": "claude-code-setup",
  "version": "0.1.1",
  "private": true,
  "description": "Shared Claude Code setup: skills, an audit flow, and a read-policy hook, distributed as a plugin.",
  "type": "module",
  "scripts": {
    "test": "node --test \"skills/audit-flow/test/*.test.mjs\""
  },
  "keywords": ["claude-code", "claude-code-plugin"]
}
